using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Marker script used to tag game objects as indestructible.
/// This can be used for static objects like terrain, walls, or background props.
/// </summary>
public class Indestructible : MonoBehaviour
{
    // No behavior yet, but can be extended later for logic like collision filtering
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
